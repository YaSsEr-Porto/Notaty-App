<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ToDoList" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->

        <item name="backgroundColor">#FFFFFF</item>
        <item name="colorOnBackground">#000000</item>
    </style>

    <style name="Theme.MyApp" parent="Theme.Material3.DayNight">
        <item name="colorSurface">@color/background_dark</item>
        <item name="colorOnSurface">@color/text_light</item>
    </style>

    <style name="CustomPopupMenuTextStyle" parent="TextAppearance.Material3.BodyLarge">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="CustomPopupMenu" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColor">@color/white</item>
    </style>

</resources>